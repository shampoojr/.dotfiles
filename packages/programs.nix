{config, pkgs,...}:{
  programs = {
    steam = {
      enable = true;
    };
    java = {
      enable = true;
    };
    thunar = {
      enable = true;
    };
    direnv={
      enable = true;
    };
  };
}